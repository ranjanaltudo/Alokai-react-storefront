import { ReactNode, PropsWithChildren, ElementType } from 'react';
import { PropsWithStyle } from '../../index.ts';
import { SfListItemSize } from '@storefront-ui/shared';

export { SfListItemSize };
export interface SfListItemProps extends PropsWithChildren, PropsWithStyle {
    size?: `${SfListItemSize}`;
    disabled?: boolean;
    selected?: boolean;
    slotSuffix?: ReactNode;
    slotPrefix?: ReactNode;
    role?: string;
    childrenTag?: ElementType;
}
