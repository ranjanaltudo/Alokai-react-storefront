/// <reference types="react" />
import { type SfRatingButtonProps } from '@storefront-ui/react';
export default function SfRatingButton({ onChange, value, disabled, max, name, className, size, getLabelText, children, ...attributes }: SfRatingButtonProps): JSX.Element;
