import { Prettify, CSSLength } from '@storefront-ui/shared';
import { UsePopoverOptions } from '../usePopover';

export type UseTooltipOptions = Prettify<Omit<UsePopoverOptions, 'isOpen'> & {
    arrowSize?: CSSLength;
}>;
